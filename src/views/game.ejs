<% include head %>
<script type="text/javascript" src="/scripts/libs/require.js"></script>
<script>
	require.config({
		baseUrl: "/scripts",
		paths: {
			"knockout": "./libs/knockout-2.1.0",
			"socket.io": "<%= url %>/socket.io/socket.io",
			"jquery": "./libs/jquery-1.8.2.min",
			"jqueryui": "./libs/jquery-ui-1.9.2.custom.min",
			"underscore": "./libs/underscore-min"			
		},

		shim: {
			"socket.io": {
				exports: "io"
			},

			"underscore": {
				exports: "_"
			}
		}
	});

	require(["./bootstrap"], function(bootstrap) {
		 bootstrap.start("<%= url %>", {name: "<%= name %>", ip: "<%= ip %>"});
	});
	</script>

<span id="loading-game"><%= name %>, por favor, aguarde enquanto o jogo é carregado.
	<div id="animacaoTelaDeCarregamento"></div>
</span>


<% include foot %>
